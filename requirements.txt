# 核心:CLIP 雙編碼器 + 影像/文字向量
torch>=2.1
torchvision>=0.16
open_clip_torch>=2.24      # ViT-B-32 (openai) backbone

# 資料:WebDataset 串流 + Fashionpedia
webdataset>=0.2
datasets>=2.0                # HF 保底來源(build_streams_from_hf)
pandas>=2.0
numpy>=1.24
pillow>=10.0
tqdm>=4.66

# Captioning / 交叉編碼重排序(BLIP)
transformers>=4.40

# 近鄰檢索索引(兩種擇一即可;預設 faiss)
faiss-cpu>=1.8
hnswlib>=0.8

# 互動式 demo
gradio>=4.0

# 資料集 / 模型託管
huggingface_hub>=0.23
